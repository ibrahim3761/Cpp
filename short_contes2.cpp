#include <bits/stdc++.h>
using namespace std;
/*int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n,i;
        cin >> n;
        vector<int> v(n);
        long long sum=0,ans=0,avg=0;
        for (i = 0; i < n; i++)
        {
            cin>>v[i];
            sum+=v[i];
        }
        avg = sum / n;
        for ( i = 0; i < n; i++)
        {
            if(v[i] > avg)
            {
                ans+=(v[i]-avg);
            }
        }
        cout<<ans<<endl;
    }
    return 0;
}*/
